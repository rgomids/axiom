// Frozen, non-executable experiment fixture. Not Axiom product tests.

describe("RecordService", () => {
  it("returns the generated identifier from create");
  it("maps a repository duplicate error to conflict");
  it("retrieves a record from cache or PostgreSQL");
});
