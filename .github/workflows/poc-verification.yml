name: POC verification
# Bounded POC acceptance/verification Evidence; no product or release pipeline.

on:
  pull_request:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  verify:
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-24.04, macos-15]
    runs-on: ${{ matrix.os }}
    timeout-minutes: 20
    steps:
      - uses: actions/checkout@11d5960a326750d5838078e36cf38b85af677262 # v4
      - uses: actions/setup-go@40f1582b2485089dde7abd97c1529aa768e1baff # v5
        with:
          go-version-file: go.mod
          cache: true
      - name: Unit, integration and black-box tests
        run: go test -race ./...
      - name: Static and build checks
        run: |
          go vet ./...
          go build ./...
          go mod verify
      - name: Repository checks
        run: ./scripts/validate-repository.sh .
      - name: Bounded dogfooding smoke test
        run: ./scripts/dogfood-poc.sh
